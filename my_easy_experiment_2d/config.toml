# to give information about the simulations that we are running

[simulation]
input_data = "input_data/"
profiles_type =   "MIXED"
# possibilities: "DISTANT", "ADJACENT", "MIXED", "HALFMIXED"
n_runs = 1

[normalization]
use_ratio = false

